#image: gitlab/dind
#services:
#  - docker:dind

variables:
  SPRING_PROFILES_ACTIVE: gitlab-ci

stages:
  - maven-build
  - docker-build

maven-build:
  image: openjdk:11
  stage: maven-build
  script: 
      - chmod +x mvnw
      - ./mvnw clean install
  artifacts:
    paths:
      - target/*.jar
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
  tags:
    - master-k8s

docker-build:
  stage: docker-build
  script:
    - ls -l
    - ls target/
    - docker info
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
  tags:
    - master-k8s